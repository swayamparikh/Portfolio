import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { ThemeProvider } from "@/components/theme-provider";
import { Toaster } from "@/components/ui/sonner";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "ContentPilot AI — AI Social Media Content Generator",
    template: "%s — ContentPilot AI",
  },
  description:
    "Generate on-brand social media captions, hashtags, and content ideas in seconds with ContentPilot AI — the AI content agent for Instagram, LinkedIn, TikTok, X, and Facebook.",
  metadataBase: new URL("https://contentpilot.ai"),
  openGraph: {
    title: "ContentPilot AI — AI Social Media Content Generator",
    description:
      "Generate on-brand social media captions, hashtags, and content ideas in seconds.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      suppressHydrationWarning
      data-scroll-behavior="smooth"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
        <ThemeProvider
          attribute="class"
          defaultTheme="dark"
          enableSystem
          disableTransitionOnChange
        >
          {children}
          <Toaster richColors closeButton position="top-right" />
        </ThemeProvider>
      </body>
    </html>
  );
}
